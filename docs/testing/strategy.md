# Testing strategy

Unit 1 establishes executable API and web tests, static analysis, TypeScript checking and production builds. Later Milestone 1 units add database integration, authentication, permission and tenant-isolation security tests.

CI uses lockfiles and fails on lint, type, test or build errors.
