# @gridkeep/shared-types

Reserved for TypeScript types generated from the API's OpenAPI schema
(`apps/api` exposes `/openapi.json`). Not yet populated: Milestone 1's
frontend uses hand-written types in `apps/web/lib/types.ts` instead, since
the API surface is still small and changing. Wire up codegen here once the
API contract stabilizes past Milestone 2.
