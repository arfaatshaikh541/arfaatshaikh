import type { Locale } from "@world-of-islam/shared-types";import { OrganisationsPanel } from "@/components/organisations-panel";export default async function Organisations({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;return <><p className="eyebrow">Institution foundation</p><h1 className="page-title">Organisation workspaces</h1><p>Tenant boundaries are enforced by membership-derived context and organisation-aware database constraints.</p><OrganisationsPanel locale={locale}/></>}
