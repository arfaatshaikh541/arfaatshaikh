import type { Locale } from "@world-of-islam/shared-types";import { AuthShell } from "@/components/auth-shell";import { LoginForm } from "@/components/login-form";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;return <AuthShell locale={locale} title="Welcome back" description="Continue to the secure platform foundation."><LoginForm locale={locale}/></AuthShell>}
