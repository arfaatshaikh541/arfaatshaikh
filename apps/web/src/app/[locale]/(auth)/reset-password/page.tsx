import type { Locale } from "@world-of-islam/shared-types";import { AuthShell } from "@/components/auth-shell";import { TokenForm } from "@/components/token-form";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;return <AuthShell locale={locale} title="Choose a new password" description="The token is consumed once and all existing sessions are revoked."><TokenForm locale={locale} kind="reset"/></AuthShell>}
