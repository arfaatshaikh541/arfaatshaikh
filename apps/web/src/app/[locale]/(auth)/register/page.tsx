import type { Locale } from "@world-of-islam/shared-types";import { AuthShell } from "@/components/auth-shell";import { RegisterForm } from "@/components/register-form";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;return <AuthShell locale={locale} title="Create your account" description="Identity is established before any knowledge or institution features are introduced."><RegisterForm locale={locale}/></AuthShell>}
