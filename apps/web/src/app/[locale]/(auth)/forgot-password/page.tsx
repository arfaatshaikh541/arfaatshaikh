import type { Locale } from "@world-of-islam/shared-types";import { AuthShell } from "@/components/auth-shell";import { ForgotPasswordForm } from "@/components/forgot-password-form";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;return <AuthShell locale={locale} title="Reset access" description="Request a short-lived, single-use password reset token."><ForgotPasswordForm locale={locale}/></AuthShell>}
