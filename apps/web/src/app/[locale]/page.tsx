import type { Locale } from "@world-of-islam/shared-types";
import Link from "next/link";
import { Brand } from "@/components/brand";
import { LocaleSwitcher } from "@/components/locale-switcher";
import { getMessages } from "@/i18n/messages";
export default async function Home({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;const t=getMessages(locale);return <main className="landing"><header className="topbar"><Brand locale={locale}/><LocaleSwitcher locale={locale} label={t.language}/></header><section className="hero"><p className="eyebrow">Secure platform foundation</p><h1>{t.tagline}</h1><p>A calm, accessible shell for a future evidence-grounded Islamic knowledge universe. No religious corpus or generated religious answer is included yet.</p><div className="hero-actions"><Link className="button-link" href={`/${locale}/register`}>{t.register}</Link><Link className="text-link" href={`/${locale}/login`}>{t.signIn}</Link></div></section><section className="trust-grid" aria-label="Foundation principles"><article><span>01</span><h2>Traceable by design</h2><p>Future claims will retain provenance, review, and authenticity metadata.</p></article><article><span>02</span><h2>Arabic from the root</h2><p>RTL, language direction, typography, and keyboard access begin at the document boundary.</p></article><article><span>03</span><h2>AI with boundaries</h2><p>The future assistant will retrieve and validate approved evidence rather than improvise religious authority.</p></article></section></main>}
