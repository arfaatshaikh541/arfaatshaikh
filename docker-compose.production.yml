# Production overlay for docker-compose.yml — Milestone 29.
#
# This file does NOT stand alone. Apply it on top of the base file:
#
#   docker compose -f docker-compose.yml -f docker-compose.production.yml config
#   docker compose -f docker-compose.yml -f docker-compose.production.yml up -d
#
# What this changes from the base (development) file:
#
#   - api/worker/beat run with ENVIRONMENT=production, which makes
#     core.config.Settings._validate_production_safety a hard boot-time
#     gate: those containers refuse to start unless every credential below
#     has been overridden with a real value — not the development default
#     still present in docker-compose.yml. Every REQUIRED value below uses
#     Compose's `${VAR:?message}` syntax, so `docker compose config` itself
#     fails loudly, before any container starts, if the real deployment
#     hasn't set it.
#   - postgres/redis/object-storage no longer publish any port to the host
#     (`!reset []` — verified against this exact Compose version to
#     actually strip the base file's `ports:` list, not just add nothing
#     to it). Nothing outside the compose network can reach them directly;
#     only api/worker/beat, already on that network, can.
#   - api/worker/beat drop the `./apps/api:...` bind mounts the dev file
#     uses for live-reload — a production container runs the image it was
#     built as, not whatever happens to be on the host filesystem at
#     `docker compose up` time. `uvicorn` drops `--reload` for the same
#     reason.
#
# What this file deliberately does NOT do:
#
#   - It does not terminate TLS or provide a reverse proxy for api/web.
#     Something in front of this stack (a cloud load balancer, nginx,
#     Caddy) has to do that — see docs/deployment-production.md for what's
#     required there before this overlay is usable for real traffic.
#   - It does not change network segmentation (api/web/worker/beat and
#     postgres/redis/object-storage still share one Compose network) —
#     that is explicitly Milestone 33's scope, tracked separately rather
#     than folded in here.
#   - It does not run a HashiCorp Vault server itself — VAULT_HASHICORP_ADDR
#     below must point at wherever Vault actually runs (a managed cluster,
#     a separately-operated self-hosted instance), the same way this file
#     already treats TLS termination and the database as external
#     infrastructure it configures a connection to rather than provisions.
#     See docs/deployment-production.md for the Vault-side setup (enabling
#     the transit secrets engine, the policy VAULT_HASHICORP_TOKEN needs).

services:
  postgres:
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set to a real, securely-generated value for production}
    ports: !reset []

  redis:
    ports: !reset []

  object-storage:
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:?MINIO_ROOT_USER must be set to a real value for production}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:?MINIO_ROOT_PASSWORD must be set to a real, securely-generated value for production}
    ports: !reset []

  api:
    environment:
      ENVIRONMENT: production
      # Milestone 31 (finding C-01): DATABASE_URL must be the least-privilege
      # gridkeep_app role, never the superuser DATABASE_MIGRATION_URL below
      # connects as. GRIDKEEP_APP_DB_PASSWORD is what migration 34016597f04f
      # reads when creating/updating that role — it must be the same
      # password embedded in DATABASE_URL, or the app fails to authenticate
      # the moment `alembic upgrade head` finishes.
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL must point at the real production database, connecting as the least-privilege gridkeep_app role — never a superuser}
      DATABASE_MIGRATION_URL: ${DATABASE_MIGRATION_URL:?DATABASE_MIGRATION_URL must point at the real production database with real, non-default superuser credentials}
      GRIDKEEP_APP_DB_PASSWORD: ${GRIDKEEP_APP_DB_PASSWORD:?GRIDKEEP_APP_DB_PASSWORD must be set to a real, securely-generated value matching the password embedded in DATABASE_URL}
      CORS_ALLOW_ORIGINS: ${CORS_ALLOW_ORIGINS:?CORS_ALLOW_ORIGINS must be set to the real production origin(s), e.g. ["https://app.example.com"]}
      APP_BASE_URL: ${APP_BASE_URL:?APP_BASE_URL must be set to the real production frontend origin}
      VAULT_LOCAL_MASTER_KEY: ${VAULT_LOCAL_MASTER_KEY:?VAULT_LOCAL_MASTER_KEY must be set to a real, securely-generated value — never the repository default}
      # Milestone 32 (finding C-02): the local envelope-encryption adapter
      # above is refused at boot in production regardless of this value —
      # VAULT_ADAPTER must be "vault", selecting the real HashiCorp Vault
      # Transit-engine adapter. VAULT_HASHICORP_TOKEN's policy needs write
      # on `transit/keys/<VAULT_HASHICORP_TRANSIT_KEY_NAME>` (this service
      # self-provisions the key on first use, the same idempotent
      # create-or-converge pattern migration 34016597f04f uses for the
      # gridkeep_app Postgres role) plus encrypt/decrypt/read on the same
      # key path — never `sys/mounts` capability; enabling the transit
      # engine itself is an ops-side setup step (docs/deployment-production.md).
      VAULT_ADAPTER: ${VAULT_ADAPTER:?VAULT_ADAPTER must be set to "vault" for production — the local adapter has no real key management}
      VAULT_HASHICORP_ADDR: ${VAULT_HASHICORP_ADDR:?VAULT_HASHICORP_ADDR must point at the real production Vault server}
      VAULT_HASHICORP_TOKEN: ${VAULT_HASHICORP_TOKEN:?VAULT_HASHICORP_TOKEN must be set to a real token scoped to the transit key policy}
      # Compose merges `environment:` maps key-by-key (unlike `ports`/
      # `volumes`, it is NOT reset by simply omitting it here) — the base
      # file's dev-only ALLOW_INSECURE_COOKIES_FOR_LOCAL_DEV: "true" would
      # otherwise survive into this "production" config untouched. Settings
      # itself refuses to boot with this combination (core/config.py), but
      # this overlay should not rely on that as its only defense — it must
      # be explicitly false here so this file's own merged output is
      # correct on inspection, not just safe by accident.
      ALLOW_INSECURE_COOKIES_FOR_LOCAL_DEV: "false"
    command: >
      sh -c "alembic upgrade head &&
             python -m seed.bootstrap &&
             uvicorn main:app --host 0.0.0.0 --port 8000"
    volumes: !reset
      - gridkeep_evidence_storage:/data/evidence-storage

  worker:
    environment:
      ENVIRONMENT: production
      # Same least-privilege gridkeep_app connection as the api service
      # above — this service never runs migrations itself, so it needs no
      # GRIDKEEP_APP_DB_PASSWORD, only a DATABASE_URL whose password matches
      # what the api service's migration step already set.
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL must point at the real production database, connecting as the least-privilege gridkeep_app role — never a superuser}
      DATABASE_MIGRATION_URL: ${DATABASE_MIGRATION_URL:?DATABASE_MIGRATION_URL must point at the real production database with real, non-default superuser credentials}
      VAULT_LOCAL_MASTER_KEY: ${VAULT_LOCAL_MASTER_KEY:?VAULT_LOCAL_MASTER_KEY must be set to a real, securely-generated value — never the repository default}
      # Milestone 32 (finding C-02): same required Vault wiring as the api
      # service above — worker tasks (e.g. integration credential rotation)
      # decrypt through the same adapter.
      VAULT_ADAPTER: ${VAULT_ADAPTER:?VAULT_ADAPTER must be set to "vault" for production — the local adapter has no real key management}
      VAULT_HASHICORP_ADDR: ${VAULT_HASHICORP_ADDR:?VAULT_HASHICORP_ADDR must point at the real production Vault server}
      VAULT_HASHICORP_TOKEN: ${VAULT_HASHICORP_TOKEN:?VAULT_HASHICORP_TOKEN must be set to a real token scoped to the transit key policy}
    volumes: !reset []

  beat:
    environment:
      ENVIRONMENT: production
      DATABASE_URL: ${DATABASE_URL:?DATABASE_URL must point at the real production database, connecting as the least-privilege gridkeep_app role — never a superuser}
      DATABASE_MIGRATION_URL: ${DATABASE_MIGRATION_URL:?DATABASE_MIGRATION_URL must point at the real production database with real, non-default superuser credentials}
      VAULT_LOCAL_MASTER_KEY: ${VAULT_LOCAL_MASTER_KEY:?VAULT_LOCAL_MASTER_KEY must be set to a real, securely-generated value — never the repository default}
      # beat never itself calls the vault adapter (it only schedules tasks
      # onto Redis), but Settings' fail-closed validator still requires
      # this be set — kept for parity/consistency with the other services
      # rather than because this process's own code path needs it.
      VAULT_ADAPTER: ${VAULT_ADAPTER:?VAULT_ADAPTER must be set to "vault" for production — the local adapter has no real key management}
      VAULT_HASHICORP_ADDR: ${VAULT_HASHICORP_ADDR:?VAULT_HASHICORP_ADDR must point at the real production Vault server}
      VAULT_HASHICORP_TOKEN: ${VAULT_HASHICORP_TOKEN:?VAULT_HASHICORP_TOKEN must be set to a real token scoped to the transit key policy}
    volumes: !reset []

  web:
    build:
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:?NEXT_PUBLIC_API_BASE_URL must be set to the real production API origin}
